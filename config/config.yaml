artifacts_root: artifacts


data_ingestion:
  root_dir: artifacts/data_ingestion #where the data will be downloaded
  source_URL: https://github.com/entbappy/Branching-tutorial/raw/master/summarizer-data.zip #link where data to be downloaded is present
  local_data_file: artifacts/data_ingestion/data.zip #where the data will be downloaded in zip
  unzip_dir: artifacts/data_ingestion #where the data will be unzipped
  
data_validation:
  root_dir: artifacts/data_validation
  STATUS_FILE: artifacts/data_validation/status.txt #print the status whether the files are present or not
  ALL_REQUIRED_FILES: ["train", "test", "validation"] #to ensure all required files are present for training

data_transformation:
  root_dir: artifacts/data_transformation
  data_path: artifacts/data_ingestion/samsum_dataset
  tokenizer_name: google/pegasus-cnn_dailymail
